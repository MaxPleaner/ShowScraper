import React from 'react';

const DayGroupTitle = ({ label }) => (
  <div className='daygroup-title'>
    <span className='daygroup-title-text'>{label}</span>
  </div>
);

export default DayGroupTitle;
